# QueueStorm Warmup Classifier - environment variables
# Copy this file to `.env` and fill in your real key.
# NEVER commit a real .env to git; .env is in .gitignore.

GEMINI_API_KEY=your_key_here
PORT=8000